write function to sort the log entries by date value
    automatically sort entries every time a new entry is added?

build inventory module
    subtract components from inventory with each bottling run

